{
  "name": "sidartha",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "bundle": "npm run build && node tools/bundle-singlefile.mjs",
    "deploy": "npm run build && npx --yes firebase-tools deploy --only hosting",
    "deploy:preview": "npm run build && npx --yes firebase-tools hosting:channel:deploy preview"
  },
  "dependencies": {
    "lucide-react": "^1.26.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
